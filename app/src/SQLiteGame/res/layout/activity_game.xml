<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/activity_game_time_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/activity_game_start_btn"
        android:layout_marginBottom="49dp"

        android:layout_toLeftOf="@+id/activity_game_start_btn"
        android:layout_toStartOf="@+id/activity_game_start_btn"
        android:fontFamily="cursive"
        android:text="Timer :"
        android:textColor="#000"
        android:textSize="28sp" />

    <TextView
        android:id="@+id/activity_game_timer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/activity_game_time_tv"
        android:layout_marginLeft="10dp"
        android:layout_centerHorizontal="true"
        android:shadowColor="@android:color/holo_red_light"
        android:textColor="#000"
        android:textSize="24dp" />

    <Button
        android:id="@+id/activity_game_start_btn"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerInParent="true"
        android:text="start" />
</RelativeLayout>